* support Multi-FILE entries in cuesheet(actually this is a bug of cuesheet python module)

* support embeded image
  * For FLAC format, use 'metaflac --export-picture-to=FILE'
  * use 'metaflac --list' to see more detailed info

* support customized naming scheme

* support customized encoding argument

* use 'shntool info' to obtain detail info about chunk

* parse the output of 'shnsplit' manualy

